name: Deploy to Firebase

on:
  push:
    branches:
      - main

permissions:
  checks: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install functions dependencies
        run: cd functions && npm install

      - name: Build
        run: npm run build

      - name: Verify build output
        run: ls functions/dist/client/

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Write service account credentials
        run: printf '%s' '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CIVORA_MVP }}' > /tmp/gsa.json

      - name: Deploy hosting and functions
        run: firebase deploy --only hosting:munia-page,functions:munia --project civora-mvp
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/gsa.json
